// ignore additional modules from complexity calculation
@GC_IGNORE_MODULES:AFTER[GroundConstruction]
{
	ignore = ModuleTripLogger // what does it do, anyway?
	ignore = ModuleFuelJettison // maybe just remove station_jettison_cargo.cfg
	ignore = ModuleKISInventory // I don't really use those
	ignore = ModuleScienceExperiment // there's no science in this sandbox
	ignore = ModuleScienceContainer 
	ignore = ModuleExperienceManagement // everyone already has 5 stars
}
